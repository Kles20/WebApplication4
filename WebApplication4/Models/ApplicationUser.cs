using Microsoft.AspNetCore.Identity;

namespace WebApplication1.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Możesz dodać dodatkowe właściwości dla użytkownika, jeśli to konieczne
    }
}
